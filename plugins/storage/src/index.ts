import type {
    HarlemPlugin
} from '@harlem/core';

export default function(): HarlemPlugin {

    return {

        name: 'storage',

        install(app, eventEmitter) {
            
        }

    }

} 